# The only hard dependency.
openai

# Optional. harness.py loads .env with a built-in KEY=value parser when
# python-dotenv isn't importable, so APRIL runs on a stock python3 with nothing
# installed but openai. Install this only if you want the real thing.
python-dotenv
